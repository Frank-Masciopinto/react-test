import { Component } from '@angular/core';

@Component({
  selector: 'app-loading-spinner',
  standalone: true,
  template: ``,
})
export class LoadingSpinnerComponent {
  // TODO: show a loading indicator while users are being fetched
}
